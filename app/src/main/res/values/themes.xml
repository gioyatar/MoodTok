<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MoodTok" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <!-- Themes -->
    <style name="Theme.AppCompat.Luna" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundColor">@color/dogbrown</item>
        <item name="textFillColor">@color/black</item>
        <item name="android:fontFamily">sans-serif</item>
        <item name="colorPrimary">@color/ligghpink</item>
    </style>

    <style name="Theme.AppCompat.DevilHunter" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundColor">@color/bloodRed</item>
        <item name="textFillColor">@color/ashWhite</item>
        <item name="android:fontFamily">@font/mael</item>
        <item name="colorPrimary">@color/obsidianBlack</item>
    </style>

    <style name="Theme.AppCompat.DarkSlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="backgroundColor">@color/deepMidnightBlue</item>
        <item name="textFillColor">@color/platinumGray</item>
        <item name="android:fontFamily">@font/dmc</item>
        <item name="colorPrimary">@color/gunmetalGray</item>
    </style>

    <!-- Set the Default Theme to Devil Hunter -->
    <style name="Theme.MoodTok" parent="Theme.AppCompat.Luna" />
</resources>